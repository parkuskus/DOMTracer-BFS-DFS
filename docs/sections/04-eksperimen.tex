\section{Lingkungan dan Struktur Implementasi}

Implementasi aplikasi DOMTracer dibagi menjadi dua bagian utama, yaitu backend berbasis Go dan frontend berbasis React TypeScript. Backend bertanggung jawab atas pengambilan HTML, parsing DOM, pencocokan CSS selector, dan traversal BFS/DFS. Frontend bertanggung jawab atas antarmuka input, pengiriman request ke backend, visualisasi pohon DOM, panel metrik, dan traversal log.

\begin{table}[H]
\centering
\small
\begin{tabular}{|>{\raggedright\arraybackslash}p{0.34\textwidth}|>{\raggedright\arraybackslash}p{0.56\textwidth}|}
    \hline
    \textbf{Direktori/Berkas} & \textbf{Keterangan} \\
    \hline
    \path{backend/main.go} & Entry point HTTP API yang terhubung dengan frontend melalui endpoint \texttt{/api/traverse} dan \texttt{/api/health}. \\
    \hline
    \path{backend/cmd/server/main.go} & Entry point server alternatif yang menyediakan endpoint \texttt{/api/scrape}, \texttt{/api/tree}, \texttt{/api/scrape-upload}, dan \texttt{/api/tree-upload}. \\
    \hline
    \path{backend/cmd/cli/main.go} & Program CLI untuk menjalankan pipeline HTML, parsing, traversal, dan penyimpanan traversal log. \\
    \hline
    \path{backend/src/parser} & Modul parser HTML dan scraper URL. \\
    \hline
    \path{backend/src/selector} & Modul pencocokan CSS selector sederhana dan combinator. \\
    \hline
    \path{backend/src/traversal} & Modul BFS, DFS, metrik traversal, hasil match, dan traversal log. \\
    \hline
    \path{frontend/src} & Modul API dan tipe data TypeScript yang digunakan frontend. \\
    \hline
    \path{frontend/components} & Komponen UI untuk form input, graf pohon, tree view, panel metrik, dan log traversal. \\
    \hline
    \texttt{test} & Kumpulan file HTML dan expected output yang disiapkan sebagai bahan pengujian. \\
    \hline
\end{tabular}
\end{table}

Backend menggunakan modul Go \texttt{golang.org/x/net/html} untuk tokenisasi HTML. Frontend menggunakan Vite, React, TypeScript, Tailwind CSS, dan komponen UI pendukung. Komunikasi antara frontend dan backend dilakukan menggunakan HTTP request dengan payload dan response berformat JSON.

\section{Implementasi Backend}

\subsection{Struktur Data DOM}

Representasi DOM didefinisikan pada struct \texttt{Node} di modul \texttt{parser}. Setiap node menyimpan nama tag, teks, atribut, parent, children, dan depth. Field \texttt{Parent} diperlukan oleh selector combinator seperti child, descendant, adjacent sibling, dan general sibling. Field \texttt{Children} digunakan oleh algoritma BFS dan DFS untuk menelusuri subtree, sedangkan \texttt{Depth} digunakan untuk metrik dan visualisasi.

\begin{tcblisting}{
        colback=gray!3,
        colframe=black!60,
        title=Ringkasan Struktur Node,
        listing only,
        left=1mm,
        right=1mm,
        top=1mm,
        bottom=1mm
    }
    type Node struct {
        Tag        string
        Text       string
        Attributes map[string]string
        Parent     *Node
        Children   []*Node
        Depth      int
    }
\end{tcblisting}

\subsection{Modul Parser dan Scraper}

Modul \texttt{parser} memiliki dua fungsi utama, yaitu mengambil HTML dari URL dan mengubah HTML mentah menjadi pohon DOM. Fungsi \texttt{FetchHTML} membuat HTTP GET request dengan timeout 10 detik dan header \texttt{User-Agent} agar request lebih menyerupai browser. Fungsi \texttt{HTMLScraper} menggabungkan proses fetch dan parsing sehingga dapat langsung menghasilkan root DOM.

Parsing dilakukan oleh fungsi \texttt{ParseHTML}. Fungsi ini membuat tokenizer HTML, root semu \texttt{document}, dan stack node aktif. Ketika tokenizer membaca \texttt{StartTagToken}, parser membuat node baru, menyalin atribut, menautkannya ke parent pada puncak stack, lalu memasukkannya ke stack apabila tag tersebut bukan tag kosong. Ketika tokenizer membaca \texttt{TextToken}, teks dibersihkan dengan \texttt{TrimSpace} dan disimpan pada node aktif. Ketika tokenizer membaca \texttt{EndTagToken}, stack dipop sampai tag yang sesuai ditemukan. Untuk \texttt{SelfClosingTagToken}, node ditambahkan sebagai child tetapi tidak dimasukkan ke stack.

Daftar tag kosong seperti \texttt{img}, \texttt{br}, \texttt{hr}, \texttt{input}, \texttt{meta}, dan \texttt{link} disimpan dalam \texttt{selfClosingTags}. Dengan pendekatan ini, parser dapat membentuk relasi parent-child secara linear terhadap token HTML.

\subsection{Modul Selector}

Modul \texttt{selector} menyediakan dua tingkat pencocokan. Fungsi \texttt{MatchSelector} menangani selector sederhana, yaitu universal selector \texttt{*}, id selector \texttt{\#id}, class selector \texttt{.class}, dan tag selector. Untuk class selector, nilai atribut \texttt{class} dipisahkan menggunakan \texttt{strings.Fields} sehingga pencocokan dilakukan terhadap token class utuh.

Fungsi \texttt{Match} menangani selector relasional. Sebelum diproses, string selector dinormalisasi dengan menambahkan spasi di sekitar operator \texttt{>}, \texttt{+}, dan \texttt{\textasciitilde{}}, kemudian spasi berlebih diringkas. Setelah itu, selector dievaluasi dari sisi kanan karena node yang sedang dikunjungi adalah kandidat hasil akhir. Implementasi mendukung:

\begin{itemize}
    \item \texttt{A B}, untuk descendant combinator melalui pengecekan ancestor.
    \item \texttt{A > B}, untuk child combinator melalui pengecekan parent langsung.
    \item \texttt{A + B}, untuk adjacent sibling combinator melalui pengecekan sibling sebelumnya.
    \item \texttt{A \textasciitilde{} B}, untuk general sibling combinator melalui pengecekan semua sibling sebelumnya.
\end{itemize}

Dengan pemisahan ini, traversal tidak perlu mengetahui detail CSS selector. Traversal hanya memanggil \texttt{selector.Match(node, selector)} pada setiap node yang dikunjungi.

\subsection{Modul Traversal}

Modul \texttt{traversal} menyediakan fungsi utama \texttt{SearchDOM}. Fungsi ini menerima root DOM dan \texttt{SearchRequest}, memvalidasi selector, menentukan algoritma, lalu memanggil \texttt{searchBFS} atau \texttt{searchDFS}. Jika algoritma tidak diisi, sistem menggunakan BFS sebagai default.

Pada BFS, struktur data yang digunakan adalah queue. Root dimasukkan terlebih dahulu dengan path \texttt{0}. Setiap node diproses dari depan queue, kemudian seluruh anaknya dimasukkan ke belakang queue sesuai urutan asli. Pada DFS, struktur data yang digunakan adalah stack. Anak node dimasukkan dari kanan ke kiri agar ketika dipop, urutan kunjungan tetap kiri ke kanan.

Kedua algoritma menggunakan fungsi bersama \texttt{processVisit}. Fungsi ini memperbarui \texttt{VisitedCount}, \texttt{MaxDepthVisited}, mengevaluasi selector, menyimpan \texttt{MatchHit}, menambahkan \texttt{VisitEvent} jika log diminta, dan menghentikan traversal jika jumlah match sudah mencapai limit Top-$N$.

\begin{table}[H]
\centering
\small
\begin{tabular}{|p{0.30\textwidth}|p{0.58\textwidth}|}
    \hline
    \textbf{Field Hasil} & \textbf{Makna} \\
    \hline
    \texttt{Matches} & Daftar node yang cocok dengan selector. \\
    \hline
    \texttt{VisitedCount} & Jumlah node yang sudah dikunjungi traversal. \\
    \hline
    \texttt{MaxDepthVisited} & Kedalaman maksimum yang dicapai saat traversal. \\
    \hline
    \texttt{DurationMs} & Durasi traversal dalam milidetik. \\
    \hline
    \texttt{TraversalLog} & Daftar event kunjungan node per langkah. \\
    \hline
    \texttt{StoppedByLimit} & Bernilai true jika traversal berhenti karena Top-$N$. \\
    \hline
\end{tabular}
\end{table}

\subsection{HTTP API}

Untuk integrasi dengan frontend, server pada \texttt{backend/main.go} menyediakan endpoint \texttt{POST /api/traverse}. Endpoint ini menerima input berupa URL atau HTML mentah, mode input, algoritma, CSS selector, dan limit hasil. Setelah menerima request, server menjalankan pipeline berikut:

\begin{enumerate}
    \item Validasi field \texttt{input} dan \texttt{cssSelector}.
    \item Jika \texttt{inputMode = html}, input langsung diparse dengan \texttt{ParseHTML}; selain itu input dianggap sebagai URL dan diproses dengan \texttt{HTMLScraper}.
    \item Jalankan \texttt{SearchDOM} dengan traversal log selalu diaktifkan untuk kebutuhan visualisasi.
    \item Bentuk set \texttt{traversedPaths} dan \texttt{matchedPaths} dari hasil traversal.
    \item Konversi pohon \texttt{parser.Node} menjadi \texttt{treeNodeJSON} yang sudah memiliki \texttt{isTraversed}, \texttt{isMatched}, dan \texttt{nodeId}.
    \item Konversi traversal log backend menjadi format log frontend.
    \item Kirim response JSON berisi tree, traversal log, metrik, algoritma, dan selector.
\end{enumerate}

\begin{tcblisting}{
        colback=gray!3,
        colframe=black!60,
        title=Contoh Request POST /api/traverse,
        listing only,
        left=1mm,
        right=1mm,
        top=1mm,
        bottom=1mm
    }
    {
      "input": "https://example.com",
      "inputMode": "url",
      "algorithm": "BFS",
      "cssSelector": "a",
      "limit": 10
    }
\end{tcblisting}

\begin{tcblisting}{
        colback=gray!3,
        colframe=black!60,
        title=Ringkasan Response Frontend,
        listing only,
        left=1mm,
        right=1mm,
        top=1mm,
        bottom=1mm
    }
    {
      "tree": { "...": "annotated DOM tree" },
      "traversalLog": [],
      "metrics": {
        "searchTimeMs": 0,
        "visitedNodeCount": 0,
        "matchedNodeCount": 0,
        "maxDepth": 0
      },
      "algorithm": "BFS",
      "selector": "a"
    }
\end{tcblisting}

Selain endpoint utama tersebut, server alternatif pada \texttt{backend/cmd/server/main.go} menyediakan endpoint yang lebih eksplisit untuk kebutuhan scraping, tree, dan upload file. Endpoint ini berguna apabila pengujian backend ingin dilakukan langsung menggunakan \texttt{curl} atau tool API tanpa frontend.

\section{Implementasi Frontend}

Frontend dibangun sebagai aplikasi satu halaman. Komponen \texttt{Index.tsx} menjadi halaman utama yang mengatur state hasil traversal, status loading, error, tab aktif, dan status koneksi backend. Saat halaman dibuka, frontend memanggil \texttt{/api/health} untuk memastikan backend aktif.

\subsection{Form Input}

Komponen \texttt{InputForm.tsx} menyediakan konfigurasi traversal. Pengguna dapat memilih sumber input berupa URL, raw HTML, atau file HTML. Untuk mode file, browser membaca file dengan \texttt{FileReader} lalu mengirim isi file sebagai input HTML. Pengguna juga memilih algoritma BFS/DFS, CSS selector, dan batas hasil berupa semua hasil atau Top-$N$.

State form dikirim ke fungsi \texttt{runTraversal} pada \texttt{frontend/src/api.ts}. Fungsi ini membentuk payload \texttt{TraverseRequest}. Jika mode input adalah file, frontend mengirimnya sebagai mode \texttt{html} karena isi file sudah dibaca menjadi string.

\subsection{Integrasi API}

Modul \texttt{api.ts} menggunakan environment variable \texttt{VITE\_API\_BASE\_URL}. Pada mode pengembangan, nilai ini dapat dikosongkan karena Vite melakukan proxy untuk seluruh request \texttt{/api/*} dari frontend menuju backend pada \texttt{http://localhost:8080}. Dengan konfigurasi ini, frontend berjalan pada \texttt{http://localhost:5173}, sedangkan backend tetap berjalan pada port \texttt{8080}. Fungsi \texttt{runTraversal} mengirim request ke \texttt{/api/traverse}. Jika backend tidak dapat dihubungi, frontend menampilkan pesan error agar pengguna mengetahui bahwa server perlu dijalankan.

Tipe data response didefinisikan pada \texttt{frontend/src/types.ts}. Interface \texttt{ApiResponse} berisi \texttt{tree}, \texttt{traversalLog}, \texttt{metrics}, \texttt{algorithm}, dan \texttt{selector}. Shape ini disamakan dengan response dari \texttt{backend/main.go}.

\subsection{Visualisasi Pohon dan Log}

Komponen \texttt{TreeGraph.tsx} menampilkan DOM sebagai graf SVG. Layout pohon dihitung berdasarkan jumlah leaf pada setiap subtree. Setiap node memiliki koordinat \texttt{x} dan \texttt{y}, lalu edge digambar sebagai kurva antar parent dan child. Node yang cocok dengan selector diberi warna berbeda melalui field \texttt{isMatched}. Komponen ini juga menggunakan \texttt{traversalLog} untuk menjalankan animasi penelusuran. Pada saat animasi berjalan, node yang sedang dikunjungi diberi warna hijau terang, sedangkan setelah animasi selesai node match kembali ditampilkan dengan warna biru. Pengguna dapat menjalankan, menghentikan, melakukan step, melewati animasi, mengatur kecepatan, memperbesar atau memperkecil graf, serta membuka graf dalam mode fullscreen.

Komponen \texttt{TreeNode.tsx} menampilkan struktur DOM dalam bentuk tree view yang dapat dibuka dan ditutup. Node yang sudah dikunjungi diberi style berbeda melalui \texttt{isTraversed}, sedangkan node yang cocok diberi penanda match. Komponen ini juga menampilkan sebagian atribut dan potongan teks node.

Komponen \texttt{TraversalLog.tsx} menampilkan setiap langkah traversal pada tab terpisah. Log berisi nomor step, tag node, path node, pesan kunjungan, dan status match. Komponen ini juga menyediakan filter untuk menampilkan hanya node yang match.

\subsection{Panel Metrik}

Komponen \texttt{MetricsPanel.tsx} menampilkan empat metrik utama, yaitu waktu pencarian, jumlah node yang dikunjungi, jumlah match, dan kedalaman maksimum. Metrik ini berasal dari hasil traversal backend dan digunakan untuk membandingkan perilaku BFS dan DFS pada input yang sama.

\section{Cara Menjalankan Aplikasi}

Backend yang sesuai dengan endpoint frontend dapat dijalankan dari direktori \texttt{backend} dengan perintah berikut.

\begin{tcblisting}{
        colback=gray!3,
        colframe=black!60,
        title=Menjalankan Backend Utama,
        listing only,
        left=1mm,
        right=1mm,
        top=1mm,
        bottom=1mm
    }
    go run ./main.go
\end{tcblisting}

Frontend dapat dijalankan dari direktori \texttt{frontend} dengan perintah berikut.

\begin{tcblisting}{
        colback=gray!3,
        colframe=black!60,
        title=Menjalankan Frontend,
        listing only,
        left=1mm,
        right=1mm,
        top=1mm,
        bottom=1mm
    }
    npm run dev
\end{tcblisting}

Setelah frontend berjalan, aplikasi dapat diakses melalui \texttt{http://localhost:5173}. Request API dari frontend akan diteruskan oleh proxy Vite ke backend pada \texttt{http://localhost:8080}. Konfigurasi ini mencegah bentrok port antara server frontend dan server backend.

Apabila ingin menjalankan server alternatif yang menyediakan endpoint \texttt{/api/scrape} dan \texttt{/api/tree}, perintah yang digunakan adalah sebagai berikut.

\begin{tcblisting}{
        colback=gray!3,
        colframe=black!60,
        title=Menjalankan Server Alternatif,
        listing only,
        left=1mm,
        right=1mm,
        top=1mm,
        bottom=1mm
    }
    go run ./cmd/server/main.go
\end{tcblisting}

\section{Pengujian Penelusuran Node dan CSS Selector}

Pengujian dilakukan menggunakan berkas HTML pada direktori \texttt{test/cases} dan keluaran acuan pada direktori \texttt{test/expected}. Setiap test case dijalankan melalui aplikasi DOMTracer, kemudian hasil visualisasi disimpan sebagai gambar PNG pada direktori \texttt{docs/output}. Fokus pengujian hanya mencakup penelusuran node DOM dan pencocokan CSS selector. Pengujian API, backend secara terpisah, dan frontend secara terpisah tidak dibahas pada bagian ini.

Seluruh hasil pengujian sesuai dengan expected output. Kesesuaian dinilai dari jumlah node yang cocok, jumlah node yang dikunjungi, kedalaman maksimum, status berhenti karena limit, serta perilaku error untuk input yang memang dirancang gagal.

Secara teknis, setiap test case mengikuti pipeline yang sama. Pertama, file HTML dibaca sebagai input dan diparse menjadi pohon DOM. Kedua, traversal BFS atau DFS dijalankan sesuai spesifikasi test case. Ketiga, pada setiap node yang dikunjungi, sistem mengevaluasi CSS selector menggunakan modul selector. Keempat, sistem mencatat metrik traversal berupa jumlah node yang dikunjungi, jumlah node yang cocok, kedalaman maksimum yang dicapai, dan status \textit{stopped by limit}. Nilai-nilai tersebut kemudian dibandingkan dengan file expected JSON yang bersesuaian. Screenshot pada kolom hasil real menunjukkan kondisi aplikasi setelah test dijalankan. Untuk \texttt{case\_11}, gambar graph dan log dicantumkan bersama karena dokumen HTML berukuran lebih besar sehingga validasi visual struktur graf dan urutan traversal log sama-sama relevan.

Kolom expected pada tabel merangkum nilai acuan dari direktori \texttt{test/expected}. Kolom spesifikasi tes berisi algoritma, CSS selector, dan limit node yang digunakan. Baris hasil real menampilkan screenshot aktual dari direktori \texttt{docs/output}, sedangkan penjelasan singkat menerangkan aspek teknis yang divalidasi oleh test case tersebut.

\renewcommand{\arraystretch}{1.15}
\newcommand{\hasilGraph}[2]{%
\multicolumn{3}{|p{0.94\textwidth}|}{%
\begin{minipage}{0.93\textwidth}
\centering
\includegraphics[width=0.88\textwidth]{#1}
\par\smallskip
\raggedright\textbf{Penjelasan:} #2
\end{minipage}}\\
\hline}
\newcommand{\hasilGraphLog}[3]{%
\multicolumn{3}{|p{0.94\textwidth}|}{%
\begin{minipage}{0.93\textwidth}
\centering
\textbf{Graph}\par
\includegraphics[width=0.88\textwidth]{#1}
\par\smallskip
\textbf{Log}\par
\includegraphics[width=0.88\textwidth]{#2}
\par\smallskip
\raggedright\textbf{Penjelasan:} #3
\end{minipage}}\\
\hline}
\newcommand{\hasilError}[1]{%
\multicolumn{3}{|p{0.94\textwidth}|}{%
\begin{minipage}{0.93\textwidth}
\raggedright\textbf{Hasil Real:} Keluaran berupa error validasi sehingga tidak ada visualisasi graph. \par\smallskip
\textbf{Penjelasan:} #1
\end{minipage}}\\
\hline}

\begin{longtable}{|>{\raggedright\arraybackslash}p{0.22\textwidth}|>{\raggedright\arraybackslash}p{0.31\textwidth}|>{\raggedright\arraybackslash}p{0.33\textwidth}|}
\caption{Hasil pengujian penelusuran node DOM dan CSS selector.}\\
\hline
\textbf{Testcase} & \textbf{Spesifikasi Tes} & \textbf{Expected} \\
\hline
\endfirsthead
\endhead

\texttt{case\_1.html} &
Algoritma: BFS\newline
CSS selector: \texttt{article}\newline
Limit node: semua &
2 match, 5 node dikunjungi, kedalaman maksimum 3, tidak berhenti karena limit. \\
\hline
\hasilGraph{output/no1.png}{Tag selector dievaluasi dengan membandingkan nilai \texttt{node.Tag} terhadap selector \texttt{article}. Traversal BFS mengunjungi node per level, lalu menandai dua node \texttt{article} sebagai match tanpa memicu kondisi berhenti dini.}

\texttt{case\_2.html} &
Algoritma: BFS\newline
CSS selector: \texttt{img + p}\newline
Limit node: semua &
1 match, 5 node dikunjungi, kedalaman maksimum 3, tidak berhenti karena limit. \\
\hline
\hasilGraph{output/no2.png}{Selector \texttt{img + p} memvalidasi relasi sibling langsung. Saat node \texttt{p} dikunjungi, modul selector memeriksa sibling sebelumnya pada parent yang sama dan memastikan node tersebut adalah \texttt{img}.}

\texttt{case\_3.html} &
Algoritma: DFS\newline
CSS selector: \texttt{.entry}\newline
Limit node: 2 &
2 match, 5 node dikunjungi, kedalaman maksimum 3, berhenti karena Top-$N$. \\
\hline
\hasilGraph{output/no3.png}{Class selector membaca atribut \texttt{class}, memecahnya menjadi token dengan pemisah spasi, lalu mencari token \texttt{entry}. Karena limit Top-$N$ bernilai 2, DFS berhenti segera setelah match kedua ditemukan dan field \texttt{stoppedByLimit} bernilai benar.}

\texttt{case\_4.html} &
Algoritma: BFS\newline
CSS selector: \texttt{*}\newline
Limit node: semua &
Menghasilkan error \texttt{empty document}. \\
\hline
\hasilGraph{output/no4.png}{Kasus ini menguji validasi parser sebelum traversal. Karena tokenizer tidak menghasilkan root DOM yang valid, backend mengembalikan error \texttt{empty document}; akibatnya modul BFS/DFS dan selector matching tidak dijalankan.}

\texttt{case\_5.html} &
Algoritma: BFS\newline
CSS selector: \texttt{.featured}\newline
Limit node: semua &
1 match, 3 node dikunjungi, kedalaman maksimum 2, tidak berhenti karena limit. \\
\hline
\hasilGraph{output/no5.png}{Kasus ini memastikan normalisasi atribut berjalan benar. Nilai atribut \texttt{class} yang memiliki spasi berlebih tetap diproses menggunakan token class utuh, sehingga \texttt{.featured} hanya cocok pada node yang benar.}

\texttt{case\_6.html} &
Algoritma: BFS\newline
CSS selector: \texttt{img + p}\newline
Limit node: semua &
0 match, 5 node dikunjungi, kedalaman maksimum 3, tidak berhenti karena limit. \\
\hline
\hasilGraph{output/no6.png}{Selector \texttt{img + p} sengaja diuji pada struktur sibling yang tidak memenuhi urutan. Meskipun semua node dikunjungi, pengecekan sibling sebelumnya untuk setiap kandidat \texttt{p} tidak menemukan \texttt{img}, sehingga jumlah match tetap nol.}

\texttt{case\_7.html} &
Algoritma: BFS\newline
CSS selector: \texttt{div, p}\newline
Limit node: semua &
0 match, 4 node dikunjungi, kedalaman maksimum 2, tidak berhenti karena limit. \\
\hline
\hasilGraph{output/no7.png}{Input \texttt{div, p} merepresentasikan selector list CSS. Bentuk ini belum diparse sebagai dua selector terpisah oleh implementasi, sehingga string selector diperlakukan sebagai pola yang tidak cocok dengan node mana pun.}

\texttt{case\_8.html} &
Algoritma: DFS\newline
CSS selector: \texttt{*}\newline
Limit node: semua &
4 match, 4 node dikunjungi, kedalaman maksimum 3, tidak berhenti karena limit. \\
\hline
\hasilGraph{output/no8.png}{Kasus ini menguji toleransi parser terhadap HTML yang tidak tertutup sempurna. Stack parser tetap mampu membentuk struktur DOM, kemudian universal selector \texttt{*} mengembalikan match untuk setiap node yang dikunjungi DFS.}

\texttt{case\_9.html} &
Algoritma: DFS\newline
CSS selector: \texttt{.entry}\newline
Limit node: 10 &
2 match, 5 node dikunjungi, kedalaman maksimum 3, tidak berhenti karena limit. \\
\hline
\hasilGraph{output/no9.png}{Limit Top-$N$ bernilai 10, tetapi jumlah match aktual hanya 2. Karena kondisi berhenti hanya aktif ketika jumlah match mencapai limit, DFS tetap menelusuri seluruh node sampai stack kosong dan \texttt{stoppedByLimit} tetap salah.}

\texttt{case\_10.html} &
Algoritma: \texttt{RANDOM}\newline
CSS selector: \texttt{article}\newline
Limit node: semua &
Menghasilkan error yang memuat pesan \texttt{unsupported algorithm}. \\
\hline
\hasilError{Validasi parameter algoritma dilakukan sebelum traversal dimulai. Nilai \texttt{RANDOM} tidak termasuk pilihan legal, sehingga sistem mengembalikan error \texttt{unsupported algorithm} tanpa membentuk hasil graph.}

\texttt{case\_11.html} (11a) &
Algoritma: BFS\newline
CSS selector: \texttt{article}\newline
Limit node: semua &
20 match, 541 node dikunjungi, kedalaman maksimum 13, tidak berhenti karena limit. \\
\hline
\hasilGraphLog{output/no11a.png}{output/no11a-log.png}{Pada dokumen besar, tag selector \texttt{article} menemukan seluruh elemen produk pada halaman Books to Scrape. Log menunjukkan BFS tetap mengunjungi seluruh 541 node karena tidak ada limit, sedangkan graph menampilkan posisi match di dalam struktur DOM yang dalam.}

\texttt{case\_11.html} (11a2) &
Algoritma: BFS\newline
CSS selector: \texttt{h3 a}\newline
Limit node: semua &
20 match, 541 node dikunjungi, kedalaman maksimum 13, tidak berhenti karena limit. \\
\hline
\hasilGraphLog{output/no11a2.png}{output/no11a2-log.png}{Selector \texttt{h3 a} menguji descendant combinator pada dokumen besar. Untuk setiap kandidat \texttt{a}, modul selector menelusuri ancestor sampai menemukan \texttt{h3}, sehingga seluruh tautan judul buku terdeteksi sebagai match.}

\texttt{case\_11.html} (11a3) &
Algoritma: DFS\newline
CSS selector: \texttt{.price\_color}\newline
Limit node: 5 &
5 match, 239 node dikunjungi, kedalaman maksimum 13, berhenti karena Top-$N$. \\
\hline
\hasilGraphLog{output/no11a3.png}{output/no11a3-log.png}{Class selector \texttt{.price\_color} diuji dengan DFS dan limit 5. Urutan pre-order DFS menyebabkan traversal masuk ke subtree produk secara mendalam, lalu berhenti saat match kelima ditemukan setelah 239 node dikunjungi.}

\texttt{case\_11.html} (11b) &
Algoritma: BFS\newline
CSS selector: \texttt{.star-rating}\newline
Limit node: semua &
20 match, 541 node dikunjungi, kedalaman maksimum 13, tidak berhenti karena limit. \\
\hline
\hasilGraphLog{output/no11b.png}{output/no11b-log.png}{Selector \texttt{.star-rating} memvalidasi pencocokan class pada banyak node dengan atribut class gabungan. Karena limit tidak diaktifkan, BFS menyelesaikan traversal seluruh dokumen dan menghasilkan 20 match sesuai expected.}

\texttt{case\_11.html} (11c) &
Algoritma: DFS\newline
CSS selector: \texttt{.btn}\newline
Limit node: semua &
20 match, 541 node dikunjungi, kedalaman maksimum 13, tidak berhenti karena limit. \\
\hline
\hasilGraphLog{output/no11c.png}{output/no11c-log.png}{Selector \texttt{.btn} dijalankan dengan DFS tanpa limit pada dokumen besar. Hasil ini menunjukkan bahwa meskipun urutan kunjungan DFS berbeda dari BFS, jumlah match akhir tetap sama ketika seluruh node selesai ditelusuri.}

\texttt{case\_12.html} &
Algoritma: BFS\newline
CSS selector: \texttt{.container > ul > li}\newline
Limit node: semua &
3 match, 14 node dikunjungi, kedalaman maksimum 4, tidak berhenti karena limit. \\
\hline
\hasilGraph{output/no12.png}{Chained child combinator menguji tiga kondisi relasional sekaligus: node hasil harus berupa \texttt{li}, parent langsungnya harus \texttt{ul}, dan \texttt{ul} tersebut harus menjadi child langsung dari elemen dengan class \texttt{container}. Tiga \texttt{li} pada \texttt{ul} di dalam \texttt{div.container} cocok, sedangkan \texttt{li} di dalam \texttt{ol} dan \texttt{li} pada \texttt{div} tanpa class \texttt{container} tidak ikut terpilih.}

\texttt{case\_13.html} &
Algoritma: BFS\newline
CSS selector: \texttt{h1 \textasciitilde{} p}\newline
Limit node: semua &
3 match, 10 node dikunjungi, kedalaman maksimum 2, tidak berhenti karena limit. \\
\hline
\hasilGraph{output/no13.png}{General sibling combinator memilih seluruh elemen \texttt{p} yang berada setelah \texttt{h1} pada parent yang sama. Berbeda dari adjacent sibling, elemen \texttt{p} tidak harus tepat berada setelah \texttt{h1}; karena itu paragraf kedua dan paragraf setelah \texttt{span} tetap dianggap match selama memiliki sibling \texttt{h1} sebelumnya.}

\texttt{case\_14.html} &
Algoritma: BFS\newline
CSS selector: \texttt{\#special}\newline
Limit node: semua &
1 match, elemen \texttt{div} dengan \texttt{id="special"}, kedalaman maksimum 3, tidak berhenti karena limit. \\
\hline
\hasilGraph{output/no14.png}{ID selector memeriksa atribut \texttt{id} secara langsung dan hanya mencocokkan node dengan nilai \texttt{special}. Elemen \texttt{div} yang juga memiliki class \texttt{highlight btn} menjadi satu-satunya match; elemen tanpa atribut \texttt{id} dan elemen dengan \texttt{id="other"} tidak ikut dipilih. Tampilan aplikasi menghitung elemen \texttt{meta} sebagai node visual tambahan, tetapi hasil match selector tetap sesuai expected.}
\end{longtable}

Berdasarkan tabel pengujian, implementasi mampu menangani selector dasar, combinator, selector yang tidak didukung, HTML kosong, HTML tidak sempurna, serta limit Top-$N$. Pada kasus tanpa limit, traversal mengunjungi seluruh node yang relevan dan jumlah match sesuai expected. Pada kasus Top-$N$, traversal berhenti hanya ketika jumlah match telah mencapai batas yang diberikan.
